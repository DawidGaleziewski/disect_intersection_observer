intersection observers are a API that allows us when webpage DOM elements intersect with the page

Important thing is that intersectionObserver will fire as soon as the page loads and fire if any of the items is intersecting
